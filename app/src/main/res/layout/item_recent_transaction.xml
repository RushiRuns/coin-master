<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp"
    android:background="?android:attr/selectableItemBackground">

    <!-- Transaction Category Color Dot (MaterialCardView circle to survive programmatic color changes) -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/view_type_dot"
        android:layout_width="12dp"
        android:layout_height="12dp"
        app:cardCornerRadius="6dp"
        app:cardElevation="0dp"
        app:strokeWidth="0dp"
        app:cardBackgroundColor="@color/secondary"
        app:layout_constraintTop_toTopOf="@id/tv_transaction_title"
        app:layout_constraintBottom_toBottomOf="@id/tv_transaction_title"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Transaction Title (Body 1) -->
    <TextView
        android:id="@+id/tv_transaction_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:text="Groceries"
        android:textColor="@color/text_primary"
        android:textSize="16sp"
        android:textStyle="bold"
        android:ellipsize="end"
        android:maxLines="1"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@id/view_type_dot"
        app:layout_constraintEnd_toStartOf="@id/tv_transaction_amount" />

    <!-- Transaction Subtitle (Caption) -->
    <TextView
        android:id="@+id/tv_transaction_subtitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="2dp"
        android:layout_marginEnd="16dp"
        android:text="Bank Account - Weekly food run"
        android:textColor="@color/text_secondary"
        android:textSize="12sp"
        android:ellipsize="end"
        android:maxLines="1"
        app:layout_constraintTop_toBottomOf="@id/tv_transaction_title"
        app:layout_constraintStart_toEndOf="@id/view_type_dot"
        app:layout_constraintEnd_toStartOf="@id/tv_transaction_amount" />

    <!-- Transaction Date (Caption) -->
    <TextView
        android:id="@+id/tv_transaction_date"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="2dp"
        android:layout_marginEnd="16dp"
        android:text="Jun 06, 2026"
        android:textColor="@color/text_secondary"
        android:textSize="12sp"
        app:layout_constraintTop_toBottomOf="@id/tv_transaction_subtitle"
        app:layout_constraintStart_toEndOf="@id/view_type_dot"
        app:layout_constraintEnd_toStartOf="@id/tv_transaction_amount" />

    <!-- Transaction Amount (Body 1) -->
    <TextView
        android:id="@+id/tv_transaction_amount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="- ₹500.00"
        android:textColor="@color/error"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
